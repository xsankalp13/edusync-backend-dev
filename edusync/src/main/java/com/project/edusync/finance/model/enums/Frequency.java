package com.project.edusync.finance.model.enums;

/**
 * Represents the payment frequency for a fee particular.
 * Mapped to the ENUM('one_time', 'monthly', 'quarterly', 'annual') column.
 */
public enum Frequency {
    ONE_TIME,
    MONTHLY,
    QUARTERLY,
    ANNUAL
}
