package com.project.edusync.finance.model.enums;

/**
 * Represents the type of late fee to be applied.
 * Mapped to the ENUM('FIXED', 'PERCENTAGE') column.
 */
public enum FineType {
    FIXED,
    PERCENTAGE
}
