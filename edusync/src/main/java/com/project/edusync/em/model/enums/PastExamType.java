package com.project.edusync.em.model.enums;
/**
 * Defines the type for a past paper, which has a slightly different
 * set of values than an active exam (e.g., 'OTHER').
 */
public enum PastExamType {
    MIDTERM,
    FINAL,
    UNIT_TEST,
    OTHER
}
